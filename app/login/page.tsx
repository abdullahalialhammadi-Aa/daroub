import {AuthEntry} from '@/components/auth-entry';
export const metadata={title:'Sign in | Daroub',robots:{index:false,follow:false}};
export default function Page(){return <AuthEntry mode="login"/>}
